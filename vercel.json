{
  "version": 2,
  "builds": [
    {
      "src": "writers-app/package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "prisma generate && next build"
      }
    }
  ]
}
